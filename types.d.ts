/// <reference types="vite-plugin-svgr/client" />

type VitepressOptions = {
    root: string;
    react?: boolean;
    preact?: boolean;
}

export default vitepress = (options: VitepressOptions): Promise => {

}